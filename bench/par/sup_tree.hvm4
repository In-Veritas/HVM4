// Generate a sup tree to depth 22, with numbers at the leaves
// Tests the performance of task distribution for normalization.
// Performance is expected to increase with more threads.
// wnf is very fast for all nodes, most work is overhead of eval_normalize.
@tree = λ{
  0: λn.n;
  _: λ&d.λ&n. &L{@tree(d-1, (2*n)+0), @tree(d-1, (2*n)+1)}
}

@main = @tree(22, 1)
